<?xml version="1.0" encoding="utf-8"?>
<!--
    order_history_card_item.xml — One card row in Order History.
    Gold circle icon on the left, order details in the center,
    total amount on the right.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/colorWhite">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <!-- Gold circle with order icon inside -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp">

            <!-- Gold circle background -->
            <View
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/circle_gold" />

            <!-- Order icon centered on the circle -->
            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_create_order"
                android:tint="@color/colorWhite"
                android:contentDescription="Order" />
        </FrameLayout>

        <!-- Order ID, customer name, and date stacked vertically -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp">

            <!-- Order number — bold navy -->
            <TextView
                android:id="@+id/orderTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Order #1"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@color/colorTextPrimary" />

            <!-- Customer name — gray -->
            <TextView
                android:id="@+id/orderCustomer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Customer Name"
                android:textSize="13sp"
                android:textColor="@color/colorTextSecondary"
                android:layout_marginTop="2dp" />

            <!-- Date — small, lighter gray -->
            <TextView
                android:id="@+id/orderDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2026-01-01"
                android:textSize="11sp"
                android:textColor="#999999"
                android:layout_marginTop="2dp" />
        </LinearLayout>

        <!-- Total amount — bold gold -->
        <TextView
            android:id="@+id/orderTotal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="USh 0"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/colorAccent" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
