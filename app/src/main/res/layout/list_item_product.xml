<?xml version="1.0" encoding="utf-8"?>
<!--
    list_item_product.xml — One row in the Products ListView.
    Product name (bold, navy) on top; price (gold) and stock (gray) side-by-side below.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="@color/colorWhite">

    <!-- Product name — bold navy -->
    <TextView
        android:id="@+id/textViewProductName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Product Name"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/colorTextPrimary"
        android:layout_marginBottom="4dp" />

    <!-- Price (left) and stock (right) on the same horizontal row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/textViewProductPrice"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Price: USh 0"
            android:textSize="14sp"
            android:textColor="@color/colorAccent" />

        <TextView
            android:id="@+id/textViewProductStock"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Stock: 0"
            android:textSize="14sp"
            android:textColor="@color/colorTextSecondary"
            android:gravity="end" />

    </LinearLayout>

</LinearLayout>
